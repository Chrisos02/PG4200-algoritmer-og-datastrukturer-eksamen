package Task4_QuickSort;

public enum PivotStrategy {
    FIRST,    // Uses the first element as a pivot
    LAST,     // Uses the last element as a pivot
    RANDOM    // Uses the random selected element as a pivot
}

